{
  "name": "snake",
  "version": "1.0.0",
  "description": "Snake game",
  "main": "dist/index.html",
  "author": "NerOcrO",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/NerOcrO/snake"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.5"
  },
  "devDependencies": {
    "eslint": "^8.56.0"
  },
  "scripts": {
    "dev": "rm -rf dist && parcel index.html",
    "start": "rm -rf dist && parcel build index.html --no-source-maps",
    "lint": "npx eslint javascripts"
  }
}
